package Wrapper;

public class WrapperCheck {
    public static void main(String[] args) {
        //Integer value = 10;                    //
        Number value = 10;
        if(value instanceof Integer){}
        if(value instanceof  Integer data){}
        //data is a pattern variable that has to be subclass of the variable type for ex, if the value is Number then the
        //pattern variable should be subclass which is Integer

        final Integer apple;
        //System.out.println(apple);
    }
}
